[onyo "history"]
	interactive = tig --follow
	non-interactive = git --no-pager log --follow
[onyo "assets"]
	name-format = "{type}_{make}_{model}.{serial}"
[onyo "repo"]
	version = 2
[onyo "commit"]
	auto-message = true
